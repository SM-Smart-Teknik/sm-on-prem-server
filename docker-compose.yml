version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./.env:/usr/src/app/.env
      - ./NextProject/.cookie-cache.json:/usr/src/app/NextProject/.cookie-cache.json
    restart: unless-stopped
